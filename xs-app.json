{
  "welcomeFile": "/index.html",
  "authenticationMethod": "route",
  "sessionTimeout": 1000,
  "logout": {
    "logoutEndpoint": "/do/logout",
    "logoutPage": "/index.html"
  },
  
  "routes": [
    {
      "source": "^/sap/(.*)$",
      "target": "/sap/$1",
      "destination": "abap-order-service",
      "authenticationType": "xsuaa",
      "csrfProtection": false
    },
    
    {
      "source": "^(.*)$",
      "target": "$1",
      "service": "html5-apps-repo-rt",
      "authenticationType": "xsuaa"
    }
  ]
}
